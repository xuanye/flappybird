export const STAGE_WIDTH = Math.min(innerWidth, 450) * 2;
export const STAGE_HEIGHT = Math.min(innerHeight, 750) * 2;
export const STAGE_SCALE = 0.5;

//canvas
export const CANVAS_WIDTH = STAGE_WIDTH * STAGE_SCALE ;
export const CANVAS_HEIGHT = STAGE_HEIGHT * STAGE_SCALE ;
